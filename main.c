#include <stdio.h>


int main() {
    double a, b, c, x, y;
    printf("Enter the value of a: ");
    scanf("%lf", &a);
    printf("Enter the value of b: ");
    scanf("%lf", &b);
    printf("Enter the value of c: ");
    scanf("%lf", &c);
    printf("Enter the value of x: ");
    scanf("%lf", &x);

    printf("The predicted value of y is: %.2lf\n", y);

    return 0;
}
